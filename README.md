# 🖼️ NFT Marketplace Aggregator

A unified dashboard to view and compare NFT prices across multiple marketplaces like OpenSea, Blur, and LooksRare.

## 🚀 Features
- **Price Aggregation:** Fetches real-time floor prices from different platforms.
- **Unified Interface:** View all collections in one place instead of jumping between tabs.
- **Cross-Market Comparison:** Find the best deal for any specific NFT.
- **Multi-Chain Support:** Works with Ethereum and Polygon.

## 🛠️ Tech Stack
- **APIs:** Alchemy NFT API, OpenSea API, Reservoir SDK.
- **Frontend:** Next.js / React.js, Tailwind CSS.
- **Backend:** Node.js.

## 💡 How it works
The app uses the **Reservoir Protocol** or **Alchemy SDK** to query multiple marketplaces simultaneously and normalizes the data into a single, clean format for the user.

## 🔧 Installation
1. Clone the repo: `git clone [Your-Repo-Link]`
2. Install dependencies: `npm install`
3. Add API Key: Create a `.env` file and add your `ALCHEMY_API_KEY`.
4. Start the app: `npm run dev`
